server:
  port: 9191

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hrdb?autoReconnect=true&useSSL=false
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: Root@12345

logging:
  level:
    org:
      hibernate:
        SQL: TRACE
